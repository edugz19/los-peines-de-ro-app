import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lospeinesdero.app',
  appName: 'los-peines-de-ro-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
